import 'package:flutter/material.dart';

class ProviderScannerView extends StatelessWidget {
  const ProviderScannerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("scanner"),);
  }
}
