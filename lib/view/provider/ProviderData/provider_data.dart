import 'package:flutter/material.dart';

class ProviderDataView extends StatelessWidget {
  const ProviderDataView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("Chart"),);
  }
}
