import 'package:flutter/material.dart';

class ProviderSearchView extends StatelessWidget {
  const ProviderSearchView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("search"),);
  }
}
